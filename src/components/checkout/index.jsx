import { useContext, useState } from 'react';
import { Context, Contexto } from '../../Context';

const Checkout = () => {
    const {} = useContext(Context)


    return (
        <div>estas en checkout</div>
    )
}

export default Checkout;